import { MugenScrollDataDirective } from './mugen-scroll-data.directive';

describe('MugenScrollDataDirective', () => {
  it('should create an instance', () => {
    const directive = new MugenScrollDataDirective();
    expect(directive).toBeTruthy();
  });
});
