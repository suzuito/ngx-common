import { MugenScrollTopDirective } from './mugen-scroll-top.directive';

describe('MugenScrollTopDirective', () => {
  it('should create an instance', () => {
    const directive = new MugenScrollTopDirective();
    expect(directive).toBeTruthy();
  });
});
