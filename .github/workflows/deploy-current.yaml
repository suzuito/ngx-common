name: deploy-current
on:
  push:
    tags:
      - "^v[0-9]+.[0-9]+.[0-9]+$"
jobs:
  deploy-current:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - uses: actions/setup-node@v2
        with:
          node-version: 12
      - run: npm install
      - run: echo "//registry.npmjs.org/:_authToken=$NPM_TOKEN" > ~/.npmrc
        env:
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
      - run: npm run build-dist && cd dist/ngx-mugen-scroll && npm publish
